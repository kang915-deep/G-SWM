这份项目文档专为**申请港科大（广州）李佳老师（Jia Li）课题组**设计。它结合了李老师的**图智能（Graph Intelligence）**背景与前瞻性的 **VLA/世界模型（World Models）** 方向。

该方案避开了从零训练大模型的极高难度，侧重于**“结构化先验引入”**，非常适合低年级本科生展示科研潜力和工程能力。

---

# 项目提案：G-SWM (Graph-Augmented Simple World Model)
**基于图增强的简易具身世界模型：探究物体间结构化关系对动作预测的增益**

## 1. 项目背景与动机 (Motivation)
当前的视觉-语言-动作模型 (VLA) 大多将视觉信息展平为 Token 流，这在处理复杂空间关系（如：物体堆叠、遮挡、相对方位）时存在建模效率低的问题。
本研究旨在验证：**引入显式的图结构（Scene Graph）作为归纳偏置（Inductive Bias），能够显著提升世界模型（World Model）对物理环境演变的预测能力。**

该方向高度契合 **DSAIL Lab** 在图学习领域的深厚积淀，同时探索了具身智能的前沿边界。

## 2. 核心任务定义
在一个模拟的机器人抓取场景中，给定：
1. **当前视觉观测 $O_t$**（图像）。
2. **文本指令 $L$**（例如："Pick up the red block and place it on the blue tray"）。
3. **当前动作 $A_t$**。

**模型目标：** 预测下一帧的**物体轨迹图 (Trajectory Graph)** 和 **预测图表示 $G_{t+1}$**，以此作为世界模型的简化版实现。

## 3. 技术架构 (Architecture)

项目采用模块化设计，利用 8 卡 V100 资源进行分布式训练：

### 3.1 场景图构建层 (Graph Construction)
*   **节点提取：** 使用预训练的 **Grounding DINO** 根据文本指令提取场景中关键物体的 Bounding Boxes。
*   **特征编码：** 使用 **DINOv2** 提取每个 Box 的视觉特征作为节点初始向量。
*   **关系建图：** 根据物体间的欧式距离和相对位置（如：Above, Inside, Near）构建邻接矩阵，形成动态场景图 $G_t$。

### 3.2 跨模态融合层 (Reasoning Engine)
*   **图卷积模块 (GNN)：** 采用 **GAT (Graph Attention Network)** 或 **GraphSAGE** 处理场景图，更新节点特征，捕捉物体间的物理约束。
*   **指令对齐：** 利用轻量化 LLM（如 **Phi-3-mini**）提取指令特征，通过 Cross-Attention 与图特征融合。

### 3.3 动作与世界模型预测头 (Prediction Head)
*   预测每个节点在 $t+1$ 时刻的中心坐标位移。
*   **损失函数：** $\mathcal{L} = MSE(Pos_{pred}, Pos_{gt}) + \lambda \mathcal{L}_{graph\_consistency}$

## 4. 实验设置 (Experimental Setup)

### 4.1 数据集
*   使用 **RLBench** 或 **RoboCas** 仿真数据集。
*   这些数据集提供完美的物体位移标注（Ground Truth），方便快速验证算法闭环。

### 4.2 算力资源分配 (8x V100 策略)
*   **框架：** PyTorch + PyTorch Geometric (PyG)。
*   **并行策略：** 使用 `DistributedDataParallel (DDP)`。
*   **分配方案：**
    *   4张卡用于大规模数据并行，加速 GNN 和特征提取层的收敛。
    *   2张卡用于预加载和冻结的 Vision Encoder (DINOv2) 的推理缓冲。
    *   2张卡用于多尺度消融实验（对比不同图密度对模型效果的影响）。

## 5. 项目亮点 (Key Selling Points)

1.  **结构化思维：** 不同于简单的 End-to-End 黑盒模型，本项目将李老师擅长的 **图表示学习** 引入具身智能，体现了对研究方向的深度思考。
2.  **WAM 的简化实现：** 避开了生成视频的高难度任务，通过预测“图状态演变”来实现轻量级世界模型，展示了极高的科研可行性（Feasibility）。
3.  **工程落地能力：** 能够熟练调用 8 卡 V100 进行 DDP 训练、处理多模态数据对齐，证明了具备进入顶尖实验室的工程基础。

## 6. 预期成果 (Expected Outcomes)
*   一份完整的代码仓库（包含数据处理、图构建、训练脚本）。
*   对比实验数据：证明 **G-SWM** 在复杂指令下的位移预测准确度优于纯 CNN/Transformer 架构。
*   可视化 Demo：展示模型预测的未来物体运动轨迹图。

